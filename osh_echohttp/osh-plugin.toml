description = "Print the browser URL once `osh odoo` is ready to serve."
version = "0.1.0"
min_osh = "1.1"
extends = ["odoo"]

[commands]
echohttp = { hidden = true, help = "Print the Odoo URL once the server is ready." }
